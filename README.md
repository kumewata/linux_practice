# Usage

## For MacOS/Linux

1. Build Docker image

    `./setup.sh`

2. Start bash with Docker container

    `./run_bash.sh`

## For Windows

### @command prompt or PowerShell

1. Build Docker image

    `setup.bat`

2. Start bash with Docker container

    `run_bash.bat`
